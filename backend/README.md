# Legacy Alias Folder (`backend`)

The canonical Flutter backend source has moved to `mobile_api/`.

This `backend/` directory is kept temporarily as a compatibility shim for local runtime files such as:
- `.venv/`
- `cattle_backend.db`
- `env` (legacy PowerShell env file)

Use `mobile_api/` for source code and ongoing development.
